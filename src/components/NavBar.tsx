function NavBar() {
    return (
        <nav className="w-full h-16 flex gap-4 items-center mb-4">
            <div>
                <span className="font-bold">GAMERZEST</span>
                <span className="sr-only">Logo</span>
            </div>
        </nav>
    );
}

export default NavBar;
